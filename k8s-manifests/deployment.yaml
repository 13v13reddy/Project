apiVersion: v1
kind: Depolyment
metadata:
  name: devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devops
  template:
    metadata:
      labels:
        app: devops
    spec:
      containers:
        - name: devops
          image: 13v13reddy/devops:4
          ports:
            - containerPort: 5000